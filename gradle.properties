# Gradle JVM configuration
# Increase Java heap size for Gradle tasks to prevent OutOfMemoryError during SDK generation
# This is especially important for javadoc generation and the Java formatter
org.gradle.jvmargs=-Xmx4g -Xms512m
