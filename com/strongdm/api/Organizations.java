// Copyright 2020 StrongDM Inc
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

// Code generated by protogen. DO NOT EDIT.

package com.strongdm.api;

import com.strongdm.api.plumbing.OrganizationsGrpc;
import com.strongdm.api.plumbing.OrganizationsPlumbing;
import com.strongdm.api.plumbing.Plumbing;
import io.grpc.Deadline;
import io.grpc.ManagedChannel;
import java.util.concurrent.TimeUnit;

/**
 * Organizations exposes organization configuration. Most RPCs remain private to the go_private SDK;
 * public MFA management is exposed to all public SDK targets. The terraform-provider target is
 * opted out at the service level because the provider's data-source generator assumes every service
 * has a List RPC; MFA is instead surfaced via a hand-written resource template.
 */
public class Organizations {
  private final OrganizationsGrpc.OrganizationsBlockingStub stub;
  private final Client parent;
  private final Deadline deadline;

  public Organizations(ManagedChannel channel, Client client) {
    this.stub = OrganizationsGrpc.newBlockingStub(channel);
    this.parent = client;
    this.deadline = null;
  }

  private Organizations(
      OrganizationsGrpc.OrganizationsBlockingStub stub, Client client, Deadline deadline) {
    this.stub = stub;
    this.parent = client;
    this.deadline = deadline;
  }

  /**
   * This function returns a copy of the Organizations service which has the given deadline set for
   * all method calls.
   */
  public Organizations withDeadlineAfter(long duration, TimeUnit units) {
    Deadline deadline = Deadline.after(duration, units);
    return new Organizations(this.stub.withDeadline(deadline), this.parent, deadline);
  }
  /** GetMFA gets the organization's MFA configuration. */
  public OrganizationGetMFAResponse getMFA() throws RpcException {
    OrganizationsPlumbing.OrganizationGetMFARequest.Builder builder =
        OrganizationsPlumbing.OrganizationGetMFARequest.newBuilder();
    OrganizationsPlumbing.OrganizationGetMFARequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("Organizations.GetMFA", req);
    OrganizationsPlumbing.OrganizationGetMFAResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Organizations.GetMFA", req))
                .getMFA(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("Organizations.GetMFA", req, plumbingResponse);
    return Plumbing.convertOrganizationGetMFAResponseToPorcelain(plumbingResponse);
  }
  /** UpdateMFA updates the organization's MFA configuration. */
  public OrganizationUpdateMFAResponse updateMFA(MFAConfig mfa) throws RpcException {
    OrganizationsPlumbing.OrganizationUpdateMFARequest.Builder builder =
        OrganizationsPlumbing.OrganizationUpdateMFARequest.newBuilder();
    builder.setMfa(Plumbing.convertMFAConfigToPlumbing(mfa));
    OrganizationsPlumbing.OrganizationUpdateMFARequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("Organizations.UpdateMFA", req);
    OrganizationsPlumbing.OrganizationUpdateMFAResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Organizations.UpdateMFA", req))
                .updateMFA(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("Organizations.UpdateMFA", req, plumbingResponse);
    return Plumbing.convertOrganizationUpdateMFAResponseToPorcelain(plumbingResponse);
  }
  /** TestMFA validates MFA connectivity without persisting changes. */
  public OrganizationTestMFAResponse testMFA(MFAConfig mfa) throws RpcException {
    OrganizationsPlumbing.OrganizationTestMFARequest.Builder builder =
        OrganizationsPlumbing.OrganizationTestMFARequest.newBuilder();
    builder.setMfa(Plumbing.convertMFAConfigToPlumbing(mfa));
    OrganizationsPlumbing.OrganizationTestMFARequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("Organizations.TestMFA", req);
    OrganizationsPlumbing.OrganizationTestMFAResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Organizations.TestMFA", req))
                .testMFA(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("Organizations.TestMFA", req, plumbingResponse);
    return Plumbing.convertOrganizationTestMFAResponseToPorcelain(plumbingResponse);
  }
}
