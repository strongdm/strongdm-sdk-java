// Copyright 2020 StrongDM Inc
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

// Code generated by protogen. DO NOT EDIT.

package com.strongdm.api;

import com.strongdm.api.plumbing.GroupsGrpc;
import com.strongdm.api.plumbing.GroupsPlumbing;
import com.strongdm.api.plumbing.PageIterator;
import com.strongdm.api.plumbing.PageResult;
import com.strongdm.api.plumbing.Plumbing;
import com.strongdm.api.plumbing.Spec.GetRequestMetadata;
import com.strongdm.api.plumbing.Spec.ListRequestMetadata;
import io.grpc.Deadline;
import io.grpc.ManagedChannel;
import java.util.Collection;
import java.util.Iterator;
import java.util.List;
import java.util.concurrent.TimeUnit;
import java.util.function.Supplier;

/** A Group is a set of principals. */
public class Groups implements SnapshotGroups {
  private final GroupsGrpc.GroupsBlockingStub stub;
  private final Client parent;
  private final Deadline deadline;

  public Groups(ManagedChannel channel, Client client) {
    this.stub = GroupsGrpc.newBlockingStub(channel);
    this.parent = client;
    this.deadline = null;
  }

  private Groups(GroupsGrpc.GroupsBlockingStub stub, Client client, Deadline deadline) {
    this.stub = stub;
    this.parent = client;
    this.deadline = deadline;
  }

  /**
   * This function returns a copy of the Groups service which has the given deadline set for all
   * method calls.
   */
  public Groups withDeadlineAfter(long duration, TimeUnit units) {
    Deadline deadline = Deadline.after(duration, units);
    return new Groups(this.stub.withDeadline(deadline), this.parent, deadline);
  }
  /** Create registers a new Group. */
  public GroupCreateResponse create(Group group) throws RpcException {
    GroupsPlumbing.GroupCreateRequest.Builder builder =
        GroupsPlumbing.GroupCreateRequest.newBuilder();
    builder.setGroup(Plumbing.convertGroupToPlumbing(group));
    GroupsPlumbing.GroupCreateRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("Groups.Create", req);
    GroupsPlumbing.GroupCreateResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Groups.Create", req))
                .create(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("Groups.Create", req, plumbingResponse);
    return Plumbing.convertGroupCreateResponseToPorcelain(plumbingResponse);
  }
  /** */
  public GroupCreateFromRolesResponse createFromRoles(Collection<String> roleIds, boolean commit)
      throws RpcException {
    GroupsPlumbing.GroupCreateFromRolesRequest.Builder builder =
        GroupsPlumbing.GroupCreateFromRolesRequest.newBuilder();
    builder.addAllRoleIds((roleIds));
    builder.setCommit((commit));
    GroupsPlumbing.GroupCreateFromRolesRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("Groups.CreateFromRoles", req);
    GroupsPlumbing.GroupCreateFromRolesResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Groups.CreateFromRoles", req))
                .createFromRoles(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("Groups.CreateFromRoles", req, plumbingResponse);
    return Plumbing.convertGroupCreateFromRolesResponseToPorcelain(plumbingResponse);
  }
  /** Get reads one Group by ID. */
  public GroupGetResponse get(String id) throws RpcException {
    GroupsPlumbing.GroupGetRequest.Builder builder = GroupsPlumbing.GroupGetRequest.newBuilder();
    if (this.parent.snapshotDate != null) {
      GetRequestMetadata.Builder metaBuilder = GetRequestMetadata.newBuilder();
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
      builder.setMeta(metaBuilder);
    }
    builder.setId((id));
    GroupsPlumbing.GroupGetRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("Groups.Get", req);
    GroupsPlumbing.GroupGetResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Groups.Get", req))
                .get(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("Groups.Get", req, plumbingResponse);
    return Plumbing.convertGroupGetResponseToPorcelain(plumbingResponse);
  }
  /** Update replaces all the fields of a Group by ID. */
  public GroupUpdateResponse update(Group group) throws RpcException {
    GroupsPlumbing.GroupUpdateRequest.Builder builder =
        GroupsPlumbing.GroupUpdateRequest.newBuilder();
    builder.setGroup(Plumbing.convertGroupToPlumbing(group));
    GroupsPlumbing.GroupUpdateRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("Groups.Update", req);
    GroupsPlumbing.GroupUpdateResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Groups.Update", req))
                .update(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("Groups.Update", req, plumbingResponse);
    return Plumbing.convertGroupUpdateResponseToPorcelain(plumbingResponse);
  }
  /** Delete removes a Group by ID. */
  public GroupDeleteResponse delete(String id) throws RpcException {
    GroupsPlumbing.GroupDeleteRequest.Builder builder =
        GroupsPlumbing.GroupDeleteRequest.newBuilder();
    builder.setId((id));
    GroupsPlumbing.GroupDeleteRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("Groups.Delete", req);
    GroupsPlumbing.GroupDeleteResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Groups.Delete", req))
                .delete(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("Groups.Delete", req, plumbingResponse);
    return Plumbing.convertGroupDeleteResponseToPorcelain(plumbingResponse);
  }
  /** List gets a list of Groups matching a given set of criteria. */
  public Iterable<Group> list(String filter, Object... args) throws RpcException {
    GroupsPlumbing.GroupListRequest.Builder builder = GroupsPlumbing.GroupListRequest.newBuilder();
    builder.setFilter(Plumbing.quoteFilterArgs(filter, args));
    ListRequestMetadata.Builder metaBuilder = ListRequestMetadata.newBuilder();
    if (this.parent.pageLimit > 0) {
      metaBuilder.setLimit(this.parent.pageLimit);
    }
    if (this.parent.snapshotDate != null) {
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
    }
    builder.setMeta(metaBuilder);

    Supplier<PageResult<Group>> pageFetcher =
        () -> {
          // Note: this closure captures and reuses the builder to set the next page
          GroupsPlumbing.GroupListRequest req = builder.build();
          GroupsPlumbing.GroupListResponse plumbingResponse;
          int tries = 0;
          while (true) {
            try {
              plumbingResponse =
                  this.stub
                      .withCallCredentials(this.parent.getCallCredentials("Groups.List", req))
                      .list(req);
            } catch (Exception e) {
              if (this.parent.shouldRetry(tries, e, this.deadline)) {
                tries++;
                try {
                  Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
                } catch (Exception ignored) {
                }
                continue;
              }
              throw Plumbing.convertExceptionToPorcelain(e);
            }
            break;
          }

          List<Group> page =
              Plumbing.convertRepeatedGroupToPorcelain(plumbingResponse.getGroupsList());

          boolean hasNextCursor = plumbingResponse.getMeta().getNextCursor() != "";
          builder.setMeta(
              ListRequestMetadata.newBuilder()
                  .setCursor(plumbingResponse.getMeta().getNextCursor()));

          return new PageResult<Group>(page, hasNextCursor);
        };

    Iterator<Group> iterator = new PageIterator<>(pageFetcher);

    return () -> iterator;
  }
}
