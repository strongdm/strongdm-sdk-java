// Copyright 2020 StrongDM Inc
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

// Code generated by protogen. DO NOT EDIT.

package com.strongdm.api;

import com.strongdm.api.plumbing.PageIterator;
import com.strongdm.api.plumbing.PageResult;
import com.strongdm.api.plumbing.PeeringGroupResourcesGrpc;
import com.strongdm.api.plumbing.PeeringGroupResourcesPlumbing;
import com.strongdm.api.plumbing.Plumbing;
import com.strongdm.api.plumbing.Spec.GetRequestMetadata;
import com.strongdm.api.plumbing.Spec.ListRequestMetadata;
import io.grpc.Deadline;
import io.grpc.ManagedChannel;
import java.util.Iterator;
import java.util.List;
import java.util.concurrent.TimeUnit;
import java.util.function.Supplier;

/**
 * PeeringGroupResources provides the building blocks necessary to obtain attach a resource to a
 * peering group.
 */
public class PeeringGroupResources {
  private final PeeringGroupResourcesGrpc.PeeringGroupResourcesBlockingStub stub;
  private final Client parent;
  private final Deadline deadline;

  public PeeringGroupResources(ManagedChannel channel, Client client) {
    this.stub = PeeringGroupResourcesGrpc.newBlockingStub(channel);
    this.parent = client;
    this.deadline = null;
  }

  private PeeringGroupResources(
      PeeringGroupResourcesGrpc.PeeringGroupResourcesBlockingStub stub,
      Client client,
      Deadline deadline) {
    this.stub = stub;
    this.parent = client;
    this.deadline = deadline;
  }

  /**
   * This function returns a copy of the PeeringGroupResources service which has the given deadline
   * set for all method calls.
   */
  public PeeringGroupResources withDeadlineAfter(long duration, TimeUnit units) {
    Deadline deadline = Deadline.after(duration, units);
    return new PeeringGroupResources(this.stub.withDeadline(deadline), this.parent, deadline);
  }
  /** Create attaches a Resource to a PeeringGroup */
  public PeeringGroupResourceCreateResponse create(PeeringGroupResource peeringGroupResource)
      throws RpcException {
    PeeringGroupResourcesPlumbing.PeeringGroupResourceCreateRequest.Builder builder =
        PeeringGroupResourcesPlumbing.PeeringGroupResourceCreateRequest.newBuilder();
    builder.setPeeringGroupResource(
        Plumbing.convertPeeringGroupResourceToPlumbing(peeringGroupResource));
    PeeringGroupResourcesPlumbing.PeeringGroupResourceCreateRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("PeeringGroupResources.Create", req);
    PeeringGroupResourcesPlumbing.PeeringGroupResourceCreateResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(
                    this.parent.getCallCredentials("PeeringGroupResources.Create", req))
                .create(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent
            .getInterceptor()
            .executeAfter("PeeringGroupResources.Create", req, plumbingResponse);
    return Plumbing.convertPeeringGroupResourceCreateResponseToPorcelain(plumbingResponse);
  }
  /** Delete detaches a Resource to a PeeringGroup */
  public PeeringGroupResourceDeleteResponse delete(String id) throws RpcException {
    PeeringGroupResourcesPlumbing.PeeringGroupResourceDeleteRequest.Builder builder =
        PeeringGroupResourcesPlumbing.PeeringGroupResourceDeleteRequest.newBuilder();
    builder.setId((id));
    PeeringGroupResourcesPlumbing.PeeringGroupResourceDeleteRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("PeeringGroupResources.Delete", req);
    PeeringGroupResourcesPlumbing.PeeringGroupResourceDeleteResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(
                    this.parent.getCallCredentials("PeeringGroupResources.Delete", req))
                .delete(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent
            .getInterceptor()
            .executeAfter("PeeringGroupResources.Delete", req, plumbingResponse);
    return Plumbing.convertPeeringGroupResourceDeleteResponseToPorcelain(plumbingResponse);
  }
  /** Get reads the information of one peering group to resource attachment. */
  public PeeringGroupResourceGetResponse get(String id) throws RpcException {
    PeeringGroupResourcesPlumbing.PeeringGroupResourceGetRequest.Builder builder =
        PeeringGroupResourcesPlumbing.PeeringGroupResourceGetRequest.newBuilder();
    if (this.parent.snapshotDate != null) {
      GetRequestMetadata.Builder metaBuilder = GetRequestMetadata.newBuilder();
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
      builder.setMeta(metaBuilder);
    }
    builder.setId((id));
    PeeringGroupResourcesPlumbing.PeeringGroupResourceGetRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("PeeringGroupResources.Get", req);
    PeeringGroupResourcesPlumbing.PeeringGroupResourceGetResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(
                    this.parent.getCallCredentials("PeeringGroupResources.Get", req))
                .get(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent
            .getInterceptor()
            .executeAfter("PeeringGroupResources.Get", req, plumbingResponse);
    return Plumbing.convertPeeringGroupResourceGetResponseToPorcelain(plumbingResponse);
  }
  /** List gets a list of peering group resource attachments. */
  public Iterable<PeeringGroupResource> list(String filter, Object... args) throws RpcException {
    PeeringGroupResourcesPlumbing.PeeringGroupResourceListRequest.Builder builder =
        PeeringGroupResourcesPlumbing.PeeringGroupResourceListRequest.newBuilder();
    builder.setFilter(Plumbing.quoteFilterArgs(filter, args));
    ListRequestMetadata.Builder metaBuilder = ListRequestMetadata.newBuilder();
    if (this.parent.pageLimit > 0) {
      metaBuilder.setLimit(this.parent.pageLimit);
    }
    if (this.parent.snapshotDate != null) {
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
    }
    builder.setMeta(metaBuilder);

    Supplier<PageResult<PeeringGroupResource>> pageFetcher =
        () -> {
          // Note: this closure captures and reuses the builder to set the next page
          PeeringGroupResourcesPlumbing.PeeringGroupResourceListRequest req = builder.build();
          PeeringGroupResourcesPlumbing.PeeringGroupResourceListResponse plumbingResponse;
          int tries = 0;
          while (true) {
            try {
              plumbingResponse =
                  this.stub
                      .withCallCredentials(
                          this.parent.getCallCredentials("PeeringGroupResources.List", req))
                      .list(req);
            } catch (Exception e) {
              if (this.parent.shouldRetry(tries, e, this.deadline)) {
                tries++;
                try {
                  Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
                } catch (Exception ignored) {
                }
                continue;
              }
              throw Plumbing.convertExceptionToPorcelain(e);
            }
            break;
          }

          List<PeeringGroupResource> page =
              Plumbing.convertRepeatedPeeringGroupResourceToPorcelain(
                  plumbingResponse.getPeeringGroupResourcesList());

          boolean hasNextCursor = plumbingResponse.getMeta().getNextCursor() != "";
          builder.setMeta(
              ListRequestMetadata.newBuilder()
                  .setCursor(plumbingResponse.getMeta().getNextCursor()));

          return new PageResult<PeeringGroupResource>(page, hasNextCursor);
        };

    Iterator<PeeringGroupResource> iterator = new PageIterator<>(pageFetcher);

    return () -> iterator;
  }
}
