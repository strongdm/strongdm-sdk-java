// Copyright 2020 StrongDM Inc
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

// Code generated by protogen. DO NOT EDIT.

package com.strongdm.api;

import com.strongdm.api.plumbing.DiscoveryConnectorsGrpc;
import com.strongdm.api.plumbing.DiscoveryConnectorsPlumbing;
import com.strongdm.api.plumbing.PageIterator;
import com.strongdm.api.plumbing.PageResult;
import com.strongdm.api.plumbing.Plumbing;
import com.strongdm.api.plumbing.Spec.GetRequestMetadata;
import com.strongdm.api.plumbing.Spec.ListRequestMetadata;
import io.grpc.Deadline;
import io.grpc.ManagedChannel;
import java.util.Iterator;
import java.util.List;
import java.util.concurrent.TimeUnit;
import java.util.function.Supplier;

/**
 * A Discovery Connector is a configuration object for performing Resource Scans in remote systems
 * such as AWS, GCP, Azure, and other systems.
 */
public class DiscoveryConnectors implements SnapshotDiscoveryConnectors {
  private final DiscoveryConnectorsGrpc.DiscoveryConnectorsBlockingStub stub;
  private final Client parent;
  private final Deadline deadline;

  public DiscoveryConnectors(ManagedChannel channel, Client client) {
    this.stub = DiscoveryConnectorsGrpc.newBlockingStub(channel);
    this.parent = client;
    this.deadline = null;
  }

  private DiscoveryConnectors(
      DiscoveryConnectorsGrpc.DiscoveryConnectorsBlockingStub stub,
      Client client,
      Deadline deadline) {
    this.stub = stub;
    this.parent = client;
    this.deadline = deadline;
  }

  /**
   * This function returns a copy of the DiscoveryConnectors service which has the given deadline
   * set for all method calls.
   */
  public DiscoveryConnectors withDeadlineAfter(long duration, TimeUnit units) {
    Deadline deadline = Deadline.after(duration, units);
    return new DiscoveryConnectors(this.stub.withDeadline(deadline), this.parent, deadline);
  }
  /** Create adds a new Connector. */
  public ConnectorCreateResponse create(Connector connector) throws RpcException {
    DiscoveryConnectorsPlumbing.ConnectorCreateRequest.Builder builder =
        DiscoveryConnectorsPlumbing.ConnectorCreateRequest.newBuilder();
    builder.setConnector(Plumbing.convertConnectorToPlumbing(connector));
    DiscoveryConnectorsPlumbing.ConnectorCreateRequest req = builder.build();
    DiscoveryConnectorsPlumbing.ConnectorCreateResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(
                    this.parent.getCallCredentials("DiscoveryConnectors.Create", req))
                .create(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    return Plumbing.convertConnectorCreateResponseToPorcelain(plumbingResponse);
  }
  /** Get reads one Connector by ID */
  public ConnectorGetResponse get(String id) throws RpcException {
    DiscoveryConnectorsPlumbing.ConnectorGetRequest.Builder builder =
        DiscoveryConnectorsPlumbing.ConnectorGetRequest.newBuilder();
    if (this.parent.snapshotDate != null) {
      GetRequestMetadata.Builder metaBuilder = GetRequestMetadata.newBuilder();
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
      builder.setMeta(metaBuilder);
    }
    builder.setId((id));
    DiscoveryConnectorsPlumbing.ConnectorGetRequest req = builder.build();
    DiscoveryConnectorsPlumbing.ConnectorGetResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("DiscoveryConnectors.Get", req))
                .get(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    return Plumbing.convertConnectorGetResponseToPorcelain(plumbingResponse);
  }
  /** Update replaces all the fields of a Connector by ID. */
  public ConnectorUpdateResponse update(Connector connector) throws RpcException {
    DiscoveryConnectorsPlumbing.ConnectorUpdateRequest.Builder builder =
        DiscoveryConnectorsPlumbing.ConnectorUpdateRequest.newBuilder();
    builder.setConnector(Plumbing.convertConnectorToPlumbing(connector));
    DiscoveryConnectorsPlumbing.ConnectorUpdateRequest req = builder.build();
    DiscoveryConnectorsPlumbing.ConnectorUpdateResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(
                    this.parent.getCallCredentials("DiscoveryConnectors.Update", req))
                .update(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    return Plumbing.convertConnectorUpdateResponseToPorcelain(plumbingResponse);
  }
  /** Delete removes a Connector by ID. */
  public ConnectorDeleteResponse delete(String id) throws RpcException {
    DiscoveryConnectorsPlumbing.ConnectorDeleteRequest.Builder builder =
        DiscoveryConnectorsPlumbing.ConnectorDeleteRequest.newBuilder();
    builder.setId((id));
    DiscoveryConnectorsPlumbing.ConnectorDeleteRequest req = builder.build();
    DiscoveryConnectorsPlumbing.ConnectorDeleteResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(
                    this.parent.getCallCredentials("DiscoveryConnectors.Delete", req))
                .delete(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    return Plumbing.convertConnectorDeleteResponseToPorcelain(plumbingResponse);
  }
  /** List gets a list of Connectors matching a given set of criteria. */
  public Iterable<Connector> list(String filter, Object... args) throws RpcException {
    DiscoveryConnectorsPlumbing.ConnectorListRequest.Builder builder =
        DiscoveryConnectorsPlumbing.ConnectorListRequest.newBuilder();
    builder.setFilter(Plumbing.quoteFilterArgs(filter, args));
    ListRequestMetadata.Builder metaBuilder = ListRequestMetadata.newBuilder();
    if (this.parent.pageLimit > 0) {
      metaBuilder.setLimit(this.parent.pageLimit);
    }
    if (this.parent.snapshotDate != null) {
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
    }
    builder.setMeta(metaBuilder);

    Supplier<PageResult<Connector>> pageFetcher =
        () -> {
          // Note: this closure captures and reuses the builder to set the next page
          DiscoveryConnectorsPlumbing.ConnectorListRequest req = builder.build();
          DiscoveryConnectorsPlumbing.ConnectorListResponse plumbingResponse;
          int tries = 0;
          while (true) {
            try {
              plumbingResponse =
                  this.stub
                      .withCallCredentials(
                          this.parent.getCallCredentials("DiscoveryConnectors.List", req))
                      .list(req);
            } catch (Exception e) {
              if (this.parent.shouldRetry(tries, e, this.deadline)) {
                tries++;
                try {
                  Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
                } catch (Exception ignored) {
                }
                continue;
              }
              throw Plumbing.convertExceptionToPorcelain(e);
            }
            break;
          }

          List<Connector> page =
              Plumbing.convertRepeatedConnectorToPorcelain(plumbingResponse.getConnectorsList());

          boolean hasNextCursor = plumbingResponse.getMeta().getNextCursor() != "";
          builder.setMeta(
              ListRequestMetadata.newBuilder()
                  .setCursor(plumbingResponse.getMeta().getNextCursor()));

          return new PageResult<Connector>(page, hasNextCursor);
        };

    Iterator<Connector> iterator = new PageIterator<>(pageFetcher);

    return () -> iterator;
  }
}
