// Copyright 2020 StrongDM Inc
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

// Code generated by protogen. DO NOT EDIT.

package com.strongdm.api;

import com.strongdm.api.plumbing.PageIterator;
import com.strongdm.api.plumbing.PageResult;
import com.strongdm.api.plumbing.Plumbing;
import com.strongdm.api.plumbing.PoliciesGrpc;
import com.strongdm.api.plumbing.PoliciesPlumbing;
import com.strongdm.api.plumbing.Spec.GetRequestMetadata;
import com.strongdm.api.plumbing.Spec.ListRequestMetadata;
import io.grpc.Deadline;
import io.grpc.ManagedChannel;
import java.util.Iterator;
import java.util.List;
import java.util.concurrent.TimeUnit;
import java.util.function.Supplier;

/**
 * Policies are the collection of one or more statements that enforce fine-grained access control
 * for the users of an organization.
 */
public class Policies implements SnapshotPolicies {
  private final PoliciesGrpc.PoliciesBlockingStub stub;
  private final Client parent;
  private final Deadline deadline;

  public Policies(ManagedChannel channel, Client client) {
    this.stub = PoliciesGrpc.newBlockingStub(channel);
    this.parent = client;
    this.deadline = null;
  }

  private Policies(PoliciesGrpc.PoliciesBlockingStub stub, Client client, Deadline deadline) {
    this.stub = stub;
    this.parent = client;
    this.deadline = deadline;
  }

  /**
   * This function returns a copy of the Policies service which has the given deadline set for all
   * method calls.
   */
  public Policies withDeadlineAfter(long duration, TimeUnit units) {
    Deadline deadline = Deadline.after(duration, units);
    return new Policies(this.stub.withDeadline(deadline), this.parent, deadline);
  }
  /** Create creates a new Policy. */
  public PolicyCreateResponse create(Policy policy) throws RpcException {
    PoliciesPlumbing.PolicyCreateRequest.Builder builder =
        PoliciesPlumbing.PolicyCreateRequest.newBuilder();
    builder.setPolicy(Plumbing.convertPolicyToPlumbing(policy));
    PoliciesPlumbing.PolicyCreateRequest req = builder.build();
    PoliciesPlumbing.PolicyCreateResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Policies.Create", req))
                .create(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    return Plumbing.convertPolicyCreateResponseToPorcelain(plumbingResponse);
  }
  /** Delete removes a Policy by ID. */
  public PolicyDeleteResponse delete(String id) throws RpcException {
    PoliciesPlumbing.PolicyDeleteRequest.Builder builder =
        PoliciesPlumbing.PolicyDeleteRequest.newBuilder();
    builder.setId((id));
    PoliciesPlumbing.PolicyDeleteRequest req = builder.build();
    PoliciesPlumbing.PolicyDeleteResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Policies.Delete", req))
                .delete(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    return Plumbing.convertPolicyDeleteResponseToPorcelain(plumbingResponse);
  }
  /** Update replaces all the fields of a Policy by ID. */
  public PolicyUpdateResponse update(Policy policy) throws RpcException {
    PoliciesPlumbing.PolicyUpdateRequest.Builder builder =
        PoliciesPlumbing.PolicyUpdateRequest.newBuilder();
    builder.setPolicy(Plumbing.convertPolicyToPlumbing(policy));
    PoliciesPlumbing.PolicyUpdateRequest req = builder.build();
    PoliciesPlumbing.PolicyUpdateResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Policies.Update", req))
                .update(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    return Plumbing.convertPolicyUpdateResponseToPorcelain(plumbingResponse);
  }
  /** Get reads one Policy by ID. */
  public PolicyGetResponse get(String id) throws RpcException {
    PoliciesPlumbing.PolicyGetRequest.Builder builder =
        PoliciesPlumbing.PolicyGetRequest.newBuilder();
    if (this.parent.snapshotDate != null) {
      GetRequestMetadata.Builder metaBuilder = GetRequestMetadata.newBuilder();
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
      builder.setMeta(metaBuilder);
    }
    builder.setId((id));
    PoliciesPlumbing.PolicyGetRequest req = builder.build();
    PoliciesPlumbing.PolicyGetResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("Policies.Get", req))
                .get(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    return Plumbing.convertPolicyGetResponseToPorcelain(plumbingResponse);
  }
  /** List gets a list of Policy matching a given set of criteria */
  public Iterable<Policy> list(String filter, Object... args) throws RpcException {
    PoliciesPlumbing.PolicyListRequest.Builder builder =
        PoliciesPlumbing.PolicyListRequest.newBuilder();
    builder.setFilter(Plumbing.quoteFilterArgs(filter, args));
    ListRequestMetadata.Builder metaBuilder = ListRequestMetadata.newBuilder();
    if (this.parent.pageLimit > 0) {
      metaBuilder.setLimit(this.parent.pageLimit);
    }
    if (this.parent.snapshotDate != null) {
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
    }
    builder.setMeta(metaBuilder);

    Supplier<PageResult<Policy>> pageFetcher =
        () -> {
          // Note: this closure captures and reuses the builder to set the next page
          PoliciesPlumbing.PolicyListRequest req = builder.build();
          PoliciesPlumbing.PolicyListResponse plumbingResponse;
          int tries = 0;
          while (true) {
            try {
              plumbingResponse =
                  this.stub
                      .withCallCredentials(this.parent.getCallCredentials("Policies.List", req))
                      .list(req);
            } catch (Exception e) {
              if (this.parent.shouldRetry(tries, e, this.deadline)) {
                tries++;
                try {
                  Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
                } catch (Exception ignored) {
                }
                continue;
              }
              throw Plumbing.convertExceptionToPorcelain(e);
            }
            break;
          }

          List<Policy> page =
              Plumbing.convertRepeatedPolicyToPorcelain(plumbingResponse.getPoliciesList());

          boolean hasNextCursor = plumbingResponse.getMeta().getNextCursor() != "";
          builder.setMeta(
              ListRequestMetadata.newBuilder()
                  .setCursor(plumbingResponse.getMeta().getNextCursor()));

          return new PageResult<Policy>(page, hasNextCursor);
        };

    Iterator<Policy> iterator = new PageIterator<>(pageFetcher);

    return () -> iterator;
  }
}
