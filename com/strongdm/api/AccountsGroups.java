// Copyright 2020 StrongDM Inc
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

// Code generated by protogen. DO NOT EDIT.

package com.strongdm.api;

import com.strongdm.api.plumbing.AccountsGroupsGrpc;
import com.strongdm.api.plumbing.AccountsGroupsPlumbing;
import com.strongdm.api.plumbing.PageIterator;
import com.strongdm.api.plumbing.PageResult;
import com.strongdm.api.plumbing.Plumbing;
import com.strongdm.api.plumbing.Spec.GetRequestMetadata;
import com.strongdm.api.plumbing.Spec.ListRequestMetadata;
import io.grpc.Deadline;
import io.grpc.ManagedChannel;
import java.util.Iterator;
import java.util.List;
import java.util.concurrent.TimeUnit;
import java.util.function.Supplier;

/** An AccountGroup links an account and a group. */
public class AccountsGroups implements SnapshotAccountsGroups {
  private final AccountsGroupsGrpc.AccountsGroupsBlockingStub stub;
  private final Client parent;
  private final Deadline deadline;

  public AccountsGroups(ManagedChannel channel, Client client) {
    this.stub = AccountsGroupsGrpc.newBlockingStub(channel);
    this.parent = client;
    this.deadline = null;
  }

  private AccountsGroups(
      AccountsGroupsGrpc.AccountsGroupsBlockingStub stub, Client client, Deadline deadline) {
    this.stub = stub;
    this.parent = client;
    this.deadline = deadline;
  }

  /**
   * This function returns a copy of the AccountsGroups service which has the given deadline set for
   * all method calls.
   */
  public AccountsGroups withDeadlineAfter(long duration, TimeUnit units) {
    Deadline deadline = Deadline.after(duration, units);
    return new AccountsGroups(this.stub.withDeadline(deadline), this.parent, deadline);
  }
  /** Create create a new AccountGroup. */
  public AccountGroupCreateResponse create(AccountGroup accountGroup) throws RpcException {
    AccountsGroupsPlumbing.AccountGroupCreateRequest.Builder builder =
        AccountsGroupsPlumbing.AccountGroupCreateRequest.newBuilder();
    builder.setAccountGroup(Plumbing.convertAccountGroupToPlumbing(accountGroup));
    AccountsGroupsPlumbing.AccountGroupCreateRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("AccountsGroups.Create", req);
    AccountsGroupsPlumbing.AccountGroupCreateResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("AccountsGroups.Create", req))
                .create(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("AccountsGroups.Create", req, plumbingResponse);
    return Plumbing.convertAccountGroupCreateResponseToPorcelain(plumbingResponse);
  }
  /** Get reads one AccountGroup by ID. */
  public AccountGroupGetResponse get(String id) throws RpcException {
    AccountsGroupsPlumbing.AccountGroupGetRequest.Builder builder =
        AccountsGroupsPlumbing.AccountGroupGetRequest.newBuilder();
    if (this.parent.snapshotDate != null) {
      GetRequestMetadata.Builder metaBuilder = GetRequestMetadata.newBuilder();
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
      builder.setMeta(metaBuilder);
    }
    builder.setId((id));
    AccountsGroupsPlumbing.AccountGroupGetRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("AccountsGroups.Get", req);
    AccountsGroupsPlumbing.AccountGroupGetResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("AccountsGroups.Get", req))
                .get(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("AccountsGroups.Get", req, plumbingResponse);
    return Plumbing.convertAccountGroupGetResponseToPorcelain(plumbingResponse);
  }
  /** Delete removes an AccountGroup by ID. */
  public AccountGroupDeleteResponse delete(String id) throws RpcException {
    AccountsGroupsPlumbing.AccountGroupDeleteRequest.Builder builder =
        AccountsGroupsPlumbing.AccountGroupDeleteRequest.newBuilder();
    builder.setId((id));
    AccountsGroupsPlumbing.AccountGroupDeleteRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("AccountsGroups.Delete", req);
    AccountsGroupsPlumbing.AccountGroupDeleteResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("AccountsGroups.Delete", req))
                .delete(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("AccountsGroups.Delete", req, plumbingResponse);
    return Plumbing.convertAccountGroupDeleteResponseToPorcelain(plumbingResponse);
  }
  /** List gets a list of AccountGroups matching a given set of criteria. */
  public Iterable<AccountGroup> list(String filter, Object... args) throws RpcException {
    AccountsGroupsPlumbing.AccountGroupListRequest.Builder builder =
        AccountsGroupsPlumbing.AccountGroupListRequest.newBuilder();
    builder.setFilter(Plumbing.quoteFilterArgs(filter, args));
    ListRequestMetadata.Builder metaBuilder = ListRequestMetadata.newBuilder();
    if (this.parent.pageLimit > 0) {
      metaBuilder.setLimit(this.parent.pageLimit);
    }
    if (this.parent.snapshotDate != null) {
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
    }
    builder.setMeta(metaBuilder);

    Supplier<PageResult<AccountGroup>> pageFetcher =
        () -> {
          // Note: this closure captures and reuses the builder to set the next page
          AccountsGroupsPlumbing.AccountGroupListRequest req = builder.build();
          AccountsGroupsPlumbing.AccountGroupListResponse plumbingResponse;
          int tries = 0;
          while (true) {
            try {
              plumbingResponse =
                  this.stub
                      .withCallCredentials(
                          this.parent.getCallCredentials("AccountsGroups.List", req))
                      .list(req);
            } catch (Exception e) {
              if (this.parent.shouldRetry(tries, e, this.deadline)) {
                tries++;
                try {
                  Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
                } catch (Exception ignored) {
                }
                continue;
              }
              throw Plumbing.convertExceptionToPorcelain(e);
            }
            break;
          }

          List<AccountGroup> page =
              Plumbing.convertRepeatedAccountGroupToPorcelain(
                  plumbingResponse.getAccountGroupsList());

          boolean hasNextCursor = plumbingResponse.getMeta().getNextCursor() != "";
          builder.setMeta(
              ListRequestMetadata.newBuilder()
                  .setCursor(plumbingResponse.getMeta().getNextCursor()));

          return new PageResult<AccountGroup>(page, hasNextCursor);
        };

    Iterator<AccountGroup> iterator = new PageIterator<>(pageFetcher);

    return () -> iterator;
  }
}
