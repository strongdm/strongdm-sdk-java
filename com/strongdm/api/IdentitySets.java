// Copyright 2020 StrongDM Inc
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

// Code generated by protogen. DO NOT EDIT.

package com.strongdm.api;

import com.strongdm.api.plumbing.IdentitySetsGrpc;
import com.strongdm.api.plumbing.IdentitySetsPlumbing;
import com.strongdm.api.plumbing.PageIterator;
import com.strongdm.api.plumbing.PageResult;
import com.strongdm.api.plumbing.Plumbing;
import com.strongdm.api.plumbing.Spec.GetRequestMetadata;
import com.strongdm.api.plumbing.Spec.ListRequestMetadata;
import io.grpc.Deadline;
import io.grpc.ManagedChannel;
import java.util.Iterator;
import java.util.List;
import java.util.concurrent.TimeUnit;
import java.util.function.Supplier;

/**
 * A IdentitySet is a named grouping of Identity Aliases for Accounts. An Account's relationship to
 * a IdentitySet is defined via IdentityAlias objects.
 */
public class IdentitySets implements SnapshotIdentitySets {
  private final IdentitySetsGrpc.IdentitySetsBlockingStub stub;
  private final Client parent;
  private final Deadline deadline;

  public IdentitySets(ManagedChannel channel, Client client) {
    this.stub = IdentitySetsGrpc.newBlockingStub(channel);
    this.parent = client;
    this.deadline = null;
  }

  private IdentitySets(
      IdentitySetsGrpc.IdentitySetsBlockingStub stub, Client client, Deadline deadline) {
    this.stub = stub;
    this.parent = client;
    this.deadline = deadline;
  }

  /**
   * This function returns a copy of the IdentitySets service which has the given deadline set for
   * all method calls.
   */
  public IdentitySets withDeadlineAfter(long duration, TimeUnit units) {
    Deadline deadline = Deadline.after(duration, units);
    return new IdentitySets(this.stub.withDeadline(deadline), this.parent, deadline);
  }
  /** Create registers a new IdentitySet. */
  public IdentitySetCreateResponse create(IdentitySet identitySet) throws RpcException {
    IdentitySetsPlumbing.IdentitySetCreateRequest.Builder builder =
        IdentitySetsPlumbing.IdentitySetCreateRequest.newBuilder();
    builder.setIdentitySet(Plumbing.convertIdentitySetToPlumbing(identitySet));
    IdentitySetsPlumbing.IdentitySetCreateRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("IdentitySets.Create", req);
    IdentitySetsPlumbing.IdentitySetCreateResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("IdentitySets.Create", req))
                .create(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("IdentitySets.Create", req, plumbingResponse);
    return Plumbing.convertIdentitySetCreateResponseToPorcelain(plumbingResponse);
  }
  /** Get reads one IdentitySet by ID. */
  public IdentitySetGetResponse get(String id) throws RpcException {
    IdentitySetsPlumbing.IdentitySetGetRequest.Builder builder =
        IdentitySetsPlumbing.IdentitySetGetRequest.newBuilder();
    if (this.parent.snapshotDate != null) {
      GetRequestMetadata.Builder metaBuilder = GetRequestMetadata.newBuilder();
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
      builder.setMeta(metaBuilder);
    }
    builder.setId((id));
    IdentitySetsPlumbing.IdentitySetGetRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("IdentitySets.Get", req);
    IdentitySetsPlumbing.IdentitySetGetResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("IdentitySets.Get", req))
                .get(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("IdentitySets.Get", req, plumbingResponse);
    return Plumbing.convertIdentitySetGetResponseToPorcelain(plumbingResponse);
  }
  /** Update replaces all the fields of a IdentitySet by ID. */
  public IdentitySetUpdateResponse update(IdentitySet identitySet) throws RpcException {
    IdentitySetsPlumbing.IdentitySetUpdateRequest.Builder builder =
        IdentitySetsPlumbing.IdentitySetUpdateRequest.newBuilder();
    builder.setIdentitySet(Plumbing.convertIdentitySetToPlumbing(identitySet));
    IdentitySetsPlumbing.IdentitySetUpdateRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("IdentitySets.Update", req);
    IdentitySetsPlumbing.IdentitySetUpdateResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("IdentitySets.Update", req))
                .update(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("IdentitySets.Update", req, plumbingResponse);
    return Plumbing.convertIdentitySetUpdateResponseToPorcelain(plumbingResponse);
  }
  /** Delete removes a IdentitySet by ID. */
  public IdentitySetDeleteResponse delete(String id) throws RpcException {
    IdentitySetsPlumbing.IdentitySetDeleteRequest.Builder builder =
        IdentitySetsPlumbing.IdentitySetDeleteRequest.newBuilder();
    builder.setId((id));
    IdentitySetsPlumbing.IdentitySetDeleteRequest req = builder.build();
    // Execute before interceptor hooks
    req = this.parent.getInterceptor().executeBefore("IdentitySets.Delete", req);
    IdentitySetsPlumbing.IdentitySetDeleteResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(this.parent.getCallCredentials("IdentitySets.Delete", req))
                .delete(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    // Execute after interceptor hooks
    plumbingResponse =
        this.parent.getInterceptor().executeAfter("IdentitySets.Delete", req, plumbingResponse);
    return Plumbing.convertIdentitySetDeleteResponseToPorcelain(plumbingResponse);
  }
  /** List gets a list of IdentitySets matching a given set of criteria. */
  public Iterable<IdentitySet> list(String filter, Object... args) throws RpcException {
    IdentitySetsPlumbing.IdentitySetListRequest.Builder builder =
        IdentitySetsPlumbing.IdentitySetListRequest.newBuilder();
    builder.setFilter(Plumbing.quoteFilterArgs(filter, args));
    ListRequestMetadata.Builder metaBuilder = ListRequestMetadata.newBuilder();
    if (this.parent.pageLimit > 0) {
      metaBuilder.setLimit(this.parent.pageLimit);
    }
    if (this.parent.snapshotDate != null) {
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
    }
    builder.setMeta(metaBuilder);

    Supplier<PageResult<IdentitySet>> pageFetcher =
        () -> {
          // Note: this closure captures and reuses the builder to set the next page
          IdentitySetsPlumbing.IdentitySetListRequest req = builder.build();
          IdentitySetsPlumbing.IdentitySetListResponse plumbingResponse;
          int tries = 0;
          while (true) {
            try {
              plumbingResponse =
                  this.stub
                      .withCallCredentials(this.parent.getCallCredentials("IdentitySets.List", req))
                      .list(req);
            } catch (Exception e) {
              if (this.parent.shouldRetry(tries, e, this.deadline)) {
                tries++;
                try {
                  Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
                } catch (Exception ignored) {
                }
                continue;
              }
              throw Plumbing.convertExceptionToPorcelain(e);
            }
            break;
          }

          List<IdentitySet> page =
              Plumbing.convertRepeatedIdentitySetToPorcelain(
                  plumbingResponse.getIdentitySetsList());

          boolean hasNextCursor = plumbingResponse.getMeta().getNextCursor() != "";
          builder.setMeta(
              ListRequestMetadata.newBuilder()
                  .setCursor(plumbingResponse.getMeta().getNextCursor()));

          return new PageResult<IdentitySet>(page, hasNextCursor);
        };

    Iterator<IdentitySet> iterator = new PageIterator<>(pageFetcher);

    return () -> iterator;
  }
}
