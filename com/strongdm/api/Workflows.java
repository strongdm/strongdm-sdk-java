// Copyright 2020 StrongDM Inc
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

// Code generated by protogen. DO NOT EDIT.

package com.strongdm.api;

import com.strongdm.api.plumbing.PageIterator;
import com.strongdm.api.plumbing.PageResult;
import com.strongdm.api.plumbing.Plumbing;
import com.strongdm.api.plumbing.Spec.ListRequestMetadata;
import com.strongdm.api.plumbing.WorkflowsGrpc;
import com.strongdm.api.plumbing.WorkflowsPlumbing;
import io.grpc.ManagedChannel;
import java.util.Iterator;
import java.util.List;
import java.util.concurrent.TimeUnit;
import java.util.function.Supplier;

/**
 * Workflows are the collection of rules that define the resources to which access can be requested,
 * the users that can request that access, and the mechanism for approving those requests which can
 * either but automatic approval or a set of users authorized to approve the requests.
 */
public class Workflows implements SnapshotWorkflows {
  private final WorkflowsGrpc.WorkflowsBlockingStub stub;
  private final Client parent;

  public Workflows(ManagedChannel channel, Client client) {

    this.stub = WorkflowsGrpc.newBlockingStub(channel);
    this.parent = client;
  }

  private Workflows(WorkflowsGrpc.WorkflowsBlockingStub stub, Client client) {
    this.stub = stub;
    this.parent = client;
  }

  /**
   * This function returns a copy of the Workflows service which has the given deadline set for all
   * method calls.
   */
  public Workflows withDeadlineAfter(long duration, TimeUnit units) {
    return new Workflows(this.stub.withDeadlineAfter(duration, units), this.parent);
  }
  /** Lists existing workflows. */
  public Iterable<Workflow> list(String filter, Object... args) throws RpcException {
    WorkflowsPlumbing.WorkflowListRequest.Builder builder =
        WorkflowsPlumbing.WorkflowListRequest.newBuilder();
    builder.setFilter(Plumbing.quoteFilterArgs(filter, args));
    ListRequestMetadata.Builder metaBuilder = ListRequestMetadata.newBuilder();
    if (this.parent.pageLimit > 0) {
      metaBuilder.setLimit(this.parent.pageLimit);
    }
    if (this.parent.snapshotDate != null) {
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
    }
    builder.setMeta(metaBuilder);

    Supplier<PageResult<Workflow>> pageFetcher =
        () -> {
          // Note: this closure captures and reuses the builder to set the next page
          WorkflowsPlumbing.WorkflowListRequest req = builder.build();
          WorkflowsPlumbing.WorkflowListResponse plumbingResponse;
          int tries = 0;
          while (true) {
            try {
              plumbingResponse =
                  this.stub
                      .withCallCredentials(this.parent.getCallCredentials("Workflows.List", req))
                      .list(req);
            } catch (Exception e) {
              if (this.parent.shouldRetry(tries, e)) {
                tries++;
                this.parent.jitterSleep(tries);
                continue;
              }
              throw Plumbing.convertExceptionToPorcelain(e);
            }
            break;
          }

          List<Workflow> page =
              Plumbing.convertRepeatedWorkflowToPorcelain(plumbingResponse.getWorkflowsList());

          boolean hasNextCursor = plumbingResponse.getMeta().getNextCursor() != "";
          builder.setMeta(
              ListRequestMetadata.newBuilder()
                  .setCursor(plumbingResponse.getMeta().getNextCursor()));

          return new PageResult<Workflow>(page, hasNextCursor);
        };

    Iterator<Workflow> iterator = new PageIterator<>(pageFetcher);

    return () -> iterator;
  }
}
