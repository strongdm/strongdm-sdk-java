// Copyright 2020 StrongDM Inc
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

// Code generated by protogen. DO NOT EDIT.

package com.strongdm.api;

import com.strongdm.api.plumbing.PageIterator;
import com.strongdm.api.plumbing.PageResult;
import com.strongdm.api.plumbing.Plumbing;
import com.strongdm.api.plumbing.RemoteIdentityGroupsGrpc;
import com.strongdm.api.plumbing.RemoteIdentityGroupsPlumbing;
import com.strongdm.api.plumbing.Spec.GetRequestMetadata;
import com.strongdm.api.plumbing.Spec.ListRequestMetadata;
import io.grpc.Deadline;
import io.grpc.ManagedChannel;
import java.util.Iterator;
import java.util.List;
import java.util.concurrent.TimeUnit;
import java.util.function.Supplier;

/**
 * A RemoteIdentityGroup is a named grouping of Remote Identities for Accounts. An Account's
 * relationship to a RemoteIdentityGroup is defined via RemoteIdentity objects.
 */
@Deprecated
public class RemoteIdentityGroups implements SnapshotRemoteIdentityGroups {
  private final RemoteIdentityGroupsGrpc.RemoteIdentityGroupsBlockingStub stub;
  private final Client parent;
  private final Deadline deadline;

  public RemoteIdentityGroups(ManagedChannel channel, Client client) {
    this.stub = RemoteIdentityGroupsGrpc.newBlockingStub(channel);
    this.parent = client;
    this.deadline = null;
  }

  private RemoteIdentityGroups(
      RemoteIdentityGroupsGrpc.RemoteIdentityGroupsBlockingStub stub,
      Client client,
      Deadline deadline) {
    this.stub = stub;
    this.parent = client;
    this.deadline = deadline;
  }

  /**
   * This function returns a copy of the RemoteIdentityGroups service which has the given deadline
   * set for all method calls.
   */
  public RemoteIdentityGroups withDeadlineAfter(long duration, TimeUnit units) {
    Deadline deadline = Deadline.after(duration, units);
    return new RemoteIdentityGroups(this.stub.withDeadline(deadline), this.parent, deadline);
  }
  /** Get reads one RemoteIdentityGroup by ID. */
  public RemoteIdentityGroupGetResponse get(String id) throws RpcException {
    RemoteIdentityGroupsPlumbing.RemoteIdentityGroupGetRequest.Builder builder =
        RemoteIdentityGroupsPlumbing.RemoteIdentityGroupGetRequest.newBuilder();
    if (this.parent.snapshotDate != null) {
      GetRequestMetadata.Builder metaBuilder = GetRequestMetadata.newBuilder();
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
      builder.setMeta(metaBuilder);
    }
    builder.setId((id));
    RemoteIdentityGroupsPlumbing.RemoteIdentityGroupGetRequest req = builder.build();
    RemoteIdentityGroupsPlumbing.RemoteIdentityGroupGetResponse plumbingResponse;
    int tries = 0;
    while (true) {
      try {
        plumbingResponse =
            this.stub
                .withCallCredentials(
                    this.parent.getCallCredentials("RemoteIdentityGroups.Get", req))
                .get(req);
      } catch (Exception e) {
        if (this.parent.shouldRetry(tries, e, this.deadline)) {
          tries++;
          try {
            Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
          } catch (Exception ignored) {
          }
          continue;
        }
        throw Plumbing.convertExceptionToPorcelain(e);
      }
      break;
    }
    return Plumbing.convertRemoteIdentityGroupGetResponseToPorcelain(plumbingResponse);
  }
  /** List gets a list of RemoteIdentityGroups matching a given set of criteria. */
  public Iterable<RemoteIdentityGroup> list(String filter, Object... args) throws RpcException {
    RemoteIdentityGroupsPlumbing.RemoteIdentityGroupListRequest.Builder builder =
        RemoteIdentityGroupsPlumbing.RemoteIdentityGroupListRequest.newBuilder();
    builder.setFilter(Plumbing.quoteFilterArgs(filter, args));
    ListRequestMetadata.Builder metaBuilder = ListRequestMetadata.newBuilder();
    if (this.parent.pageLimit > 0) {
      metaBuilder.setLimit(this.parent.pageLimit);
    }
    if (this.parent.snapshotDate != null) {
      metaBuilder.setSnapshotAt(Plumbing.convertTimestampToPlumbing(this.parent.snapshotDate));
    }
    builder.setMeta(metaBuilder);

    Supplier<PageResult<RemoteIdentityGroup>> pageFetcher =
        () -> {
          // Note: this closure captures and reuses the builder to set the next page
          RemoteIdentityGroupsPlumbing.RemoteIdentityGroupListRequest req = builder.build();
          RemoteIdentityGroupsPlumbing.RemoteIdentityGroupListResponse plumbingResponse;
          int tries = 0;
          while (true) {
            try {
              plumbingResponse =
                  this.stub
                      .withCallCredentials(
                          this.parent.getCallCredentials("RemoteIdentityGroups.List", req))
                      .list(req);
            } catch (Exception e) {
              if (this.parent.shouldRetry(tries, e, this.deadline)) {
                tries++;
                try {
                  Thread.sleep(this.parent.exponentialBackoff(tries, this.deadline));
                } catch (Exception ignored) {
                }
                continue;
              }
              throw Plumbing.convertExceptionToPorcelain(e);
            }
            break;
          }

          List<RemoteIdentityGroup> page =
              Plumbing.convertRepeatedRemoteIdentityGroupToPorcelain(
                  plumbingResponse.getRemoteIdentityGroupsList());

          boolean hasNextCursor = plumbingResponse.getMeta().getNextCursor() != "";
          builder.setMeta(
              ListRequestMetadata.newBuilder()
                  .setCursor(plumbingResponse.getMeta().getNextCursor()));

          return new PageResult<RemoteIdentityGroup>(page, hasNextCursor);
        };

    Iterator<RemoteIdentityGroup> iterator = new PageIterator<>(pageFetcher);

    return () -> iterator;
  }
}
